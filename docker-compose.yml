services:
  homelab-mcp:
    build: .
    container_name: homelab-mcp
    restart: unless-stopped
    env_file: .env
    ports:
      - "5774:5774"
    # Healthcheck comes from the image's baked-in HEALTHCHECK (see Dockerfile).
